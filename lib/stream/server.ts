/**
 * Placeholder — messaging is disabled ("Coming Soon").
 * stream-chat has been removed from dependencies to reduce bundle size.
 * Restore when re-enabling messaging.
 */
export function getStreamServerClient(): null {
  return null;
}
